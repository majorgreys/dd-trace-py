#!/bin/bash

set -o errexit
set -o pipefail
set -o nounset

mkdir -p /artifacts/output/

# cleanup any existing output artifacts
rm -f /artifacts/output/*.*

pytest --benchmark-json=/artifacts/output/encoder.json
